# Reference Tekton manifests (also rendered by charts/demo-lightwell).
# Prefer: helm upgrade --install demo charts/demo-lightwell
# Default build target = OpenShift internal registry; set quay.push=true for Quay.
apiVersion: v1
kind: List
items: []
